container = 1280;
gutter = 10px;

.limit {
  max-width: em(container);
  padding-left: gutter;
  padding-right: gutter;
  text-align: center;
  position: relative;
  margin-left: auto;
  margin-right: auto;
}
